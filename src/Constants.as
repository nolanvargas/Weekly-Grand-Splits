// 10 laps for Weekly Grand race
const int MAX_LAPS = 10;

// UI column min-widths (pixels)
const int COL_WIDTH_LAP    = 32;
const int COL_WIDTH_DELTA  = 72;
const int COL_WIDTH_TIME   = 72;
const int COL_WIDTH_CP_LAP = 16;
const int COL_WIDTH_CP_ABS = 36;
const int COL_WIDTH_CP_DELTA = 52;

// Colors
const vec4 COLOR_WHITE = vec4(1.f,  1.f,  1.f,  1.f);
const vec4 COLOR_GREEN = vec4(0.4f, 1.0f, 0.4f, 1.0f);
const vec4 COLOR_RED   = vec4(1.0f, 0.4f, 0.4f, 1.0f);
const vec4 COLOR_GOLD  = vec4(1.0f, 0.84f, 0.0f, 1.0f);
const vec4 COLOR_GRAY  = vec4(0.6f, 0.6f, 0.6f, 1.0f);
